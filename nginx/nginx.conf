events {}

http {
    server {
        listen 80;
        server_name localhost;

        location / {
            proxy_pass http://django:8000;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $host;
            proxy_redirect off;
            if (!-f $request_filename) {
                proxy_pass http://django:8000;
                break;
            }
        }

        location /static/ {
           alias /static/;
           types { text/css css; }
#            add_header Cache-Control "no-cache, no-store, must-revalidate";
#             add_header Pragma "no-cache";
#             add_header Expires 0;
        }
    }
}
# events {}
#
# http {
#     server {
#         listen 80;
#         server_name localhost;
#
#         # Главный маршрут
#         location / {
#             proxy_pass http://django:8000;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header Host $host;
#             proxy_redirect off;
#
#             # Удаляем ненужное условие с проверкой файла
#             try_files $uri @django;
#         }
#
#         location @django {
#             proxy_pass http://django:8000;
#         }
#
#         # Обслуживание статики
#         location /static/ {
#             alias /static/; # Указываем точный путь к статике в контейнере
#             autoindex on;       # Включаем отображение содержимого директории (удобно для отладки)
#             add_header Cache-Control "no-cache, no-store, must-revalidate";
#             add_header Pragma "no-cache";
#             add_header Expires 0;
#         }
#     }
# }