x-environment-defaults: &environment-defaults
  POSTGRES_DB: ${POSTGRES_DB}
  POSTGRES_USER: ${POSTGRES_USER}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  POSTGRES_HOST: ${POSTGRES_HOST}
  POSTGRES_PORT: ${POSTGRES_PORT}
  TELEGRAM_TOKEN: ${TELEGRAM_TOKEN}
#  SECRET_KEY: ${SECRET_KEY}

services:

  wisdom:
    image: postgres:latest
    container_name: wisdom
    environment:
      <<: *environment-defaults
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - wisdom-network

#  django:
#    build:
#      context: admin_panel/
#      dockerfile: Dockerfile
#    container_name: django
#    volumes:
#      - ./admin_panel:/app
#      - static_volume:/app/static  # Подключаем директорию для статики
#    ports:
#      - "8000:8000"
#    environment:
#      <<: *environment-defaults  # Включаем те же переменные окружения для Alembic
#      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}  # Использование переменных для создания URL
#    networks:
#      - wisdom-network
#    depends_on:
#      - wisdom_db

  tg:
    build:
      context: .  # Контекст сборки должен быть в корне проекта, а не внутри tg_bot
      dockerfile: tg_bot/Dockerfile
    container_name: tg
    environment:
      <<: *environment-defaults
    depends_on:
      - wisdom  # Ожидание, пока база данных будет готова
    networks:
      - wisdom-network
    restart: always  # Чтобы бот перезапускался, если что-то пойдет не так

#  nginx:
#    image: nginx:latest
#    container_name: nginx
#    volumes:
#      - ./conf/nginx.conf:/etc/nginx/nginx.conf  # Путь до файла конфигурации
#      - static_volume:/app/static  # Монтируем статику
#    ports:
#      - "80:80"
#    networks:
#      - wisdom-network
#    depends_on:
#      - django

volumes:
  pg_data:
#  static_volume:

networks:
  wisdom-network:
    driver: bridge